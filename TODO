TODO:
Add more types - better handling of subtypes (smallint, mediumint, etc.)
Add constraints if known (float vs. int, etc. )
Add validation 
Add Reflecting from row objects in addition to ResultSet (default values?)
provide for field omitting - via should_process_field_delegate
add relationship following - on simple relationships
docs
tests?
save method? $form = Form::Sensible::Form::Reflector::DBIC->create_form(...);
#...
$form->save; # ?
normalize case, fields shouldn't have to be case sensitive
